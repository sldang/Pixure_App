const mongoose = require("mongoose");
const Schema = mongoose.Schema;

const communityReportSchema = new Schema({
    

});

module.exports = mongoose.model('Community Report', communityReportSchema,'Community Report')