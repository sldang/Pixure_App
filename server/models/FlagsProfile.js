const mongoose = require("mongoose");
const Schema = mongoose.Schema;

const flagsProfileSchema = new Schema({
    

});

module.exports = mongoose.model('Flags Profile', flagsProfileSchema,'Flags Profile')