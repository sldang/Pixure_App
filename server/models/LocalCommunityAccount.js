
const mongoose = require("mongoose");
const Schema = mongoose.Schema;

const localCommunityAccountSchema = new Schema({
    

});

module.exports = mongoose.model('Local Community Account', localCommunityAccountSchema,'Local Community Account')