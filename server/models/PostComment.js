const mongoose = require("mongoose");
const Schema = mongoose.Schema;

const postCommentSchema = new Schema({
    

});

module.exports = mongoose.model('Post Comment', postCommentSchema,'Post Comment')