const mongoose = require("mongoose");
const Schema = mongoose.Schema;

const tagsProfileSchema = new Schema({
    

});

module.exports = mongoose.model('Tags Profile', tagsProfileSchema,'Tags Profile')